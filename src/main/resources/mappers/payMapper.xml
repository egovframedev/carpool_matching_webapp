<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="payment.PayMapper">
	<insert id="insertOne" parameterType="paymentDTO">
		INSERT INTO payment(
			payno, amount, apply_num, pay_date, matchno, payer, driver
		) VALUES (
			#{payno}, #{amount}, #{apply_num}, #{pay_date}, #{matchno}, #{payerNo}, #{driverNo}
		)
	</insert>
	
	<select id="selectList" resultType="paymentDTO">
		SELECT 
			payno, amount, apply_num, pay_date, matchno,
			payer AS payerNo, driver AS driverNo
		FROM payment
	  	ORDER BY pay_date DESC 
	</select>
	<select id="selectPayment">
		SELECT *
		  FROM payment
		  where payno = #{payno}
	</select>
</mapper>