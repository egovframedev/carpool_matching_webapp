<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carto.mappers.BoardMapper">

	<!-- 검색 쿼리 -->
	<sql id="search">
		<if test="searchType != null" >
			<if test="searchType == 'T'.toString()">
				AND title LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType == 'C'.toString()">
				AND content LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType == 'W'.toString()">
				AND writer LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType == 'TC'.toString()">
				AND (title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
			</if>
			<if test="searchType == 'TW'.toString()">
				AND (title LIKE CONCAT('%', #{keyword}, '%') OR writer LIKE CONCAT('%', #{keyword}, '%'))
			</if>
			<if test="searchType == 'TCW'.toString()">
				AND ( title LIKE CONCAT('%', #{keyword}, '%')
					OR content LIKE CONCAT('%', #{keyword}, '%') 
					OR writer LIKE CONCAT('%', #{keyword}, '%'))
			</if>			
		</if>
	</sql>
	
	<sql id="search2">
		<trim prefix="AND (" suffix=")" prefixOverrides="OR">
			<foreach item="type" collection="searchTypeArr">
				<trim prefix="OR">
					<choose>
						<when test="type == 'T'.toString()">
							title LIKE CONCAT('%', #{keyword}, '%')
						</when>
						<when test="type == 'C'.toString()">
							content LIKE CONCAT('%', #{keyword}, '%')
						</when>
						<when test="type == 'W'.toString()">
							writer LIKE CONCAT('%', #{keyword}, '%')
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>
	
	<select id="list" resultType="BoardDTO">
	<![CDATA[
		SELECT bno, title, content, writer, viewcnt, replycnt, regdate, grpno, step, depth, ropen, btype 
		FROM tbl_board
		WHERE bno > 0
	]]>
		<include refid="search2"/>
		ORDER BY grpno DESC, step ASC
		LIMIT #{pageStart}, ${perPageNum}
	</select>
	
	<select id="listCount" resultType="int">
	<![CDATA[
		SELECT COUNT(bno)
		FROM tbl_board
		WHERE bno > 0
	]]>	
		<include refid="search"/>
	</select>
	
	<insert id="create">
		<selectKey keyProperty="bno" resultType="int" order="BEFORE">
			SELECT IFNULL(MAX(bno), 0) + 1 FROM tbl_board
		</selectKey>
		INSERT INTO tbl_board (bno, title, content, writer, grpno)
		VALUES (#{bno}, #{title}, #{content}, #{writer}, #{bno})
	</insert>
	
	<select id="read" resultType="BoardDTO">
		SELECT bno, title, content, writer, regdate, viewcnt, replycnt, grpno, step, depth
		FROM tbl_board
		WHERE bno = #{bno}
	</select>
	
	<update id="update">
		UPDATE tbl_board
		SET title = #{title}, content = #{content}
		WHERE bno = #{bno}
	</update>
	
	<delete id="delete">
		DELETE FROM tbl_board
		WHERE bno = #{bno}
	</delete>
		
	<update id="updateReplyCnt">
		UPDATE tbl_board 
		SET replycnt = replycnt + #{amount} 
		WHERE bno = #{bno}
	</update>
	
	<update id="updateViewCnt">
		UPDATE tbl_board
		SET viewcnt = viewcnt + 1 
		WHERE bno = #{bno}
	</update>
	
	<insert id="addAttach">
		INSERT INTO tbl_attach (fullName, bno)
		VALUES (#{fullName}, #{bno})
	</insert>
	
	<select id="getAttach" resultType="string">
		SELECT fullName FROM tbl_attach
		WHERE bno = #{bno}
		ORDER BY regdate;
	</select>
	
	<delete id="deleteAttach">
		DELETE FROM tbl_attach 
		WHERE bno = #{bno}
	</delete>
	
	<insert id="replaceAttach">
		INSERT INTO tbl_attach (fullName, bno)
		VALUES (#{fullName}, #{bno})
	</insert>
</mapper>